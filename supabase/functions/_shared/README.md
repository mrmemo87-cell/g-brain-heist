# Shared Edge Function helpers

`teacherQuestionOptionBalance.ts` enforces server-side answer-position balancing for AI-generated teacher PDF MCQs. Keep this logic independent from source-question extraction so original source option order is never rewritten.
