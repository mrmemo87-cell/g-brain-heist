import { RivalryStructureCode } from '../../services/rivalryService';

export const RIVALRY_STRUCTURE_LABELS: Record<RivalryStructureCode, string> = {
  relay_core: 'Relay Core',
  cipher_vault: 'Cipher Vault',
  sentinel_grid: 'Sentinel Grid',
};
